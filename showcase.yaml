branch:
  main:
    type: runtime
    startup: |
      npm install -g coffee-script typescript
      npm ci --production
      npx tsc
    entrypoint: |
      exec node_modules/.bin/hubot -a traq -n "$HUBOT_TRAQ_NAME" "$@"
    http_proxy: 8080
